// Write a function that takes a user as an input and greets them with their name and age
let user ={
    name : "Nihar",
    age : 19
}

function greet(user) {
    console.log(`Hello ${user.name} your is ${user.age}`)
}

greet(user)